You need to run the following commands post-install under your user due to the
developer not including it within this package:

$ pip install DictObject
$ pip install luckydonald-utils

You probably need a python3 pip too.
