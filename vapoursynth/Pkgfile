# Description: Video processing framework. (git checkout)
# URL:         http://www.vapoursynth.com/
# Maintainer:  wildefyr, me at wildefyr dot net
# Depends on:  git

name=vapoursynth
version=git
release=1
source=()
branch=master

build() {
	cd $PKGMK_SOURCE_DIR

	if cd $name; then
		git fetch -q; git reset --hard origin/$branch
	else
		git clone https://github.com/vapoursynth/vapoursynth -b $branch $name
		cd $name
	fi

}
