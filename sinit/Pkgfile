# Description: Sinit using suzadu. (git checkout)
# URL:         https://github.com/kori/suzaku
# Packager:    wildefyr, me at wildefyr dot net
# Maintainer:  6c37 Team, https://github.com/6c37/crux-ports-git/issues
# Depends on:  git

name=suzadu
version=git
release=1
source=()
branch=master

build() {
	cd $PKGMK_SOURCE_DIR

	if cd $name; then
		git fetch -q; git reset --hard origin/$branch
	else
		git clone http://git.suckless.org/sinit/ -b $branch $name
		cd $name
	fi

}
